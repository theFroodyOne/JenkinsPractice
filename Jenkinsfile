pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'pwd'
                sh 'ls'
                sh 'platform-tools/adb devices'
            }
        }
    }
}
